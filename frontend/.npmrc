# Refuse to install on an unsupported Node version instead of warning.
# Without this, npm only prints a notice and the run fails later inside
# Vite with a much less obvious message.
engine-strict=true
